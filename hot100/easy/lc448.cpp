//有些出现两次，有些一次，有的没出现，找出没出现的
// 4,3,2,7,8,2,3,1
//不使用额外空间，复杂度O(n)

#include<iostream>
#include<vector>
using namespace std;

void find(vector<int>& num){
    int n = num.size();
    for(int i=0;i<n;i++){

    }
}

int main(){
    vector<int> a = {4,3,2,7,8,2,3,1};
    find(a);
    return 0;
}